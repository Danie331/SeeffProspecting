CREATE TABLE [dbo].[p24_branches] (
    [rec_id]          INT           NOT NULL,
    [license_id]      INT           NOT NULL,
    [biz_unit]        INT           NOT NULL,
    [p24_agency_name] VARCHAR (250) NOT NULL
);

