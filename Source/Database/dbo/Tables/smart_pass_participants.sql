CREATE TABLE [dbo].[smart_pass_participants] (
    [smart_pass_id]        INT          NOT NULL,
    [smart_pass_person_id] INT          NOT NULL,
    [person_type]          VARCHAR (50) NOT NULL
);

