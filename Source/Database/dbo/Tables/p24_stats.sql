CREATE TABLE [dbo].[p24_stats] (
    [year]     INT NOT NULL,
    [months]   INT NOT NULL,
    [rec_id]   INT NOT NULL,
    [listings] INT NOT NULL,
    [views]    INT NOT NULL,
    [leads]    INT NOT NULL
);

