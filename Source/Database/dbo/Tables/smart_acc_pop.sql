CREATE TABLE [dbo].[smart_acc_pop] (
    [smart_acc_pop_id] INT           IDENTITY (1, 1) NOT NULL,
    [RealName]         VARCHAR (250) NOT NULL,
    [Email]            VARCHAR (250) NOT NULL
);

