CREATE TABLE [dbo].[permission_section] (
    [permission_section_id]   INT          IDENTITY (1, 1) NOT NULL,
    [permission_section_name] VARCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED ([permission_section_id] ASC)
);

