CREATE TABLE [dbo].[property_features_type] (
    [features_type_id]   INT           IDENTITY (1, 1) NOT NULL,
    [features_type_name] NVARCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED ([features_type_id] ASC)
);

